<div class="section_centered">
<h2><%= I18n.t "edit_profile.title" %></h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form_section">
      <div class="form_row left13">
      <label><%= I18n.t "edit_profile.first_name" %></label>
      <%= f.text_field :first_name, class: 'form_input required' %>
      </div>
      <div class="form_row left13">
      <label><%= I18n.t "edit_profile.last_name" %></label>
      <%= f.text_field :last_name, class: 'form_input required' %>
      </div>
      <div class="form_row left13last">
      <label><%= I18n.t "edit_profile.email" %></label>
      <%= f.text_field :email, class: 'form_input required email' %>
      </div>
      <div class="form_row left13">
      <label><%= I18n.t "edit_profile.password" %></label>
      <%= f.password_field :password, class: 'form_input required', required: true, autocomplete: "off", required: false, placeholder: 'leave it blank if you don\'t want to change it' %>
      </div>
      <div class="form_row left13">
      <label><%= I18n.t "edit_profile.password_confirmation" %></label>
      <%= f.password_field :password_confirmation, class: 'form_input required', required: true %>
      </div>
      <div class="form_row left13last">
      <label><%= I18n.t "edit_profile.current_password" %></label>
      <%= f.password_field :current_password, class: 'form_input required', required: true, placeholder: 'we need your current password to confirm your changes' %>
      </div>
 </div>

  <div class="form-actions">
    <%= f.button :submit, t("edit_profile.update"), class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
</div>
